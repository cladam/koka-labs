# koka-labs
My explorations of the koka language


### ls tests:
```bash
echo "=== GNU ls -1a ==="
ls -1a
echo ""
echo "=== koka ls -a ==="
./run.sh ls -a
echo ""
echo "=== GNU ls -1ar ==="
ls -1ar
echo ""
echo "=== koka ls -ar ==="
./run.sh ls -ar 
```
